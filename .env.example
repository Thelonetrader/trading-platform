# IB Gateway / TWS (optional overrides — defaults in Settings UI)
IB_HOST=127.0.0.1
IB_PORT=4002
IB_CLIENT_ID=1
IB_MODE=paper
# IB_ACCOUNT_ID=

# Financial Modeling Prep — fundamentals, news, earnings (optional; prefer Settings UI in Electron)
# FMP_API_KEY=
